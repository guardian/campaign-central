import React, { PropTypes } from 'react';

const CampaignPerformanceMetrics = React.createClass({
  render () {
    return (
      <div className="performance-metrics">
        Performance metrics go here.
      </div>
    );
  }
});

export default CampaignPerformanceMetrics;
